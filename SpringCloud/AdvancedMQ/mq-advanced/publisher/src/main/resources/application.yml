logging:
  pattern:
    dateformat: HH:mm:ss:SSS
  level:
    cn.itcast: debug
spring:
  rabbitmq:
    host: 127.0.0.1 # rabbitMQ的ip地址
    port: 5672 # 端口
    username: itcast
    password: 123321
    virtual-host: /
#    publisher-confirm-type: correlated # 异步回调机制
#    publisher-returns: true # 不会直接丢弃
#    template:
#      mandatory: true # 消息回执,false则直接丢弃